name: Lighthouse audit

on:
  schedule:
    - cron: '0 6 * * 1'  # mandag 06:00 UTC
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            https://birkenborg.dev
            https://birkenborg.dev/skrifter
            https://birkenborg.dev/projekter
            https://birkenborg.dev/cv
            https://birkenborg.dev/skrifter/ma-agent-paragraf-30
          uploadArtifacts: true
          temporaryPublicStorage: true
          configPath: ./lighthouserc.json
