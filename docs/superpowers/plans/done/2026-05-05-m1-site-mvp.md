# M1 — Site MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bygge og deploye et statisk Astro-site på `*.pages.dev` med forside, skrifter, projekter, CV og kontakt — i Anthropic-æstetik fra spec'et.

**Architecture:** Astro static-site i `site/` der læser markdown-content fra repo-roden `content/` via Astro 5's content layer. Vanilla CSS med design-tokens. Cloudflare Pages deployer fra `main`-branch. Lighthouse-CI kører ugentligt via GitHub Actions.

**Tech Stack:** Astro 5, TypeScript, vanilla CSS (custom properties), Vitest, Playwright (light e2e), Cloudflare Pages, GitHub Actions.

---

## Forudsætninger

- Repository er init'et på `C:\Users\birke\Projects\birkenborg-dev\` med spec committet
- Node 22+ og Python 3.13 installeret (`where node` bekræfter)
- Ingen Astro-projekt findes endnu — vi starter på en bar mark
- Arbejdsmappe: `C:\Users\birke\Projects\birkenborg-dev\`
- Git-bruger sat (én gang globalt — ellers fejler `git commit` i alle tasks):
  ```bash
  git config --global user.name "Philip Birkenborg"
  git config --global user.email "birkenborg.p@hotmail.com"
  ```

## Filstruktur (slutresultat for M1)

```
birkenborg-dev/
├── content/                       (markdown-content, ophav for både site og senere agents)
│   ├── posts/
│   │   ├── ma-agent-paragraf-30.md
│   │   ├── gdpr-klinikkaeder.md
│   │   └── 10-agents-i-produktion.md
│   ├── projekter/
│   │   ├── retsklar.md
│   │   ├── chainhub.md
│   │   └── document-package.md
│   └── cv.md
├── site/                          (Astro-projekt)
│   ├── astro.config.mjs
│   ├── package.json
│   ├── tsconfig.json
│   ├── vitest.config.ts
│   ├── playwright.config.ts
│   ├── public/
│   │   ├── favicon.svg
│   │   └── cv.pdf                 (uploades manuelt — eksisterende CV.pdf)
│   └── src/
│       ├── pages/
│       │   ├── index.astro
│       │   ├── kontakt.astro
│       │   ├── cv.astro
│       │   ├── skrifter/
│       │   │   ├── index.astro
│       │   │   └── [slug].astro
│       │   └── projekter/
│       │       ├── index.astro
│       │       └── [slug].astro
│       ├── layouts/
│       │   └── Base.astro
│       ├── components/
│       │   ├── Header.astro
│       │   ├── Footer.astro
│       │   ├── Hero.astro
│       │   ├── StatusPanel.astro
│       │   ├── ProjectCard.astro
│       │   └── WritingItem.astro
│       ├── content.config.ts
│       ├── styles/
│       │   ├── tokens.css
│       │   ├── fonts.css
│       │   └── base.css
│       ├── lib/
│       │   ├── format-date.ts
│       │   └── format-date.test.ts
│       └── tests/
│           └── pages.spec.ts
├── docs/
│   └── superpowers/
│       ├── specs/
│       └── plans/
└── .github/
    └── workflows/
        ├── lighthouse.yml
        └── ci.yml
```

**Designprincip:** `content/` ligger i repo-roden så agents (M2) kan committe drafts uden at blande sig i Astro-projektets `src/`. Astro læser `content/` via en `glob()`-loader der peger på `../../content/`.

---

## Task 1: Initialiser Astro-projekt

**Files:**
- Create: `site/package.json`, `site/astro.config.mjs`, `site/tsconfig.json`, `site/.gitignore`

- [ ] **Step 1: Kør Astro CLI-init i `site/`**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev"
npm create astro@latest site -- --template minimal --typescript strict --install --no-git --skip-houston --yes
```

Forventet: `site/` oprettes med minimal template, dependencies installeret. Output slutter med "Liftoff confirmed."

- [ ] **Step 2: Verificér installation**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm run build
```

Forventet: build success, `dist/` oprettes med `index.html`.

- [ ] **Step 3: Tilføj root .gitignore**

Skriv `C:/Users/birke/Projects/birkenborg-dev/.gitignore`:

```
node_modules/
.DS_Store
dist/
.astro/
.env
.env.local
*.log
.cloudflare/
.brainstorm/
brainstorm-birkenborg-dev/
```

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev"
git add .gitignore site/
git commit -m "chore: scaffold astro project in site/"
```

---

## Task 2: Tilføj testværktøjer (Vitest + Playwright)

**Files:**
- Modify: `site/package.json`
- Create: `site/vitest.config.ts`, `site/playwright.config.ts`

- [ ] **Step 1: Installer dev-dependencies**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm install --save-dev vitest @vitest/ui @playwright/test
npx playwright install chromium
```

- [ ] **Step 2: Skriv `site/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['src/**/*.test.ts'],
    environment: 'node',
  },
});
```

- [ ] **Step 3: Skriv `site/playwright.config.ts`**

```ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './src/tests',
  use: { baseURL: 'http://localhost:4321' },
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:4321',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
```

- [ ] **Step 4: Tilføj scripts til `site/package.json`**

I `scripts`-objektet, tilføj:

```json
"test": "vitest run",
"test:watch": "vitest",
"e2e": "playwright test"
```

- [ ] **Step 5: Verificér Vitest kører (uden tests)**

```bash
npm run test
```

Forventet: "No test files found" — det er ok, vi har ingen endnu.

- [ ] **Step 6: Commit**

```bash
git add site/package.json site/vitest.config.ts site/playwright.config.ts site/package-lock.json
git commit -m "chore: add vitest + playwright testing tooling"
```

---

## Task 3: Design tokens (farver, mål)

**Files:**
- Create: `site/src/styles/tokens.css`

- [ ] **Step 1: Skriv `site/src/styles/tokens.css`**

```css
/* Anthropic-paletten — single source of truth */
:root {
  /* Farver */
  --cream:        #faf9f5;
  --cream-warm:   #f5f1e8;
  --gray-100:     #e8e6dc;
  --gray-200:     #d8d5c6;
  --gray-400:     #b0aea5;
  --gray-700:     #5a584f;
  --ink:          #141413;
  --clay:         #d97757;
  --clay-deep:    #b85d40;
  --clay-soft:    #e8a087;
  --slate:        #6a9bcc;
  --sage:         #788c5d;

  /* Radii */
  --r-sm: 6px;
  --r-md: 12px;
  --r-lg: 20px;

  /* Shadows */
  --shadow-sm:  0 1px 2px rgba(20,20,19,.04), 0 0 0 1px rgba(20,20,19,.06);
  --shadow-md:  0 4px 12px rgba(20,20,19,.06), 0 0 0 1px rgba(20,20,19,.06);
  --shadow-hov: 0 16px 36px rgba(20,20,19,.08), 0 0 0 1px rgba(20,20,19,.08);

  /* Layout */
  --container: 1180px;
  --pad-x: 40px;
}
```

- [ ] **Step 2: Commit**

```bash
git add site/src/styles/tokens.css
git commit -m "feat(site): add design tokens (Anthropic palette)"
```

---

## Task 4: Font-loading

**Files:**
- Create: `site/src/styles/fonts.css`

- [ ] **Step 1: Skriv `site/src/styles/fonts.css`**

```css
/* Loader Fraunces (display, italic, optical-size, SOFT),
   Source Serif 4 (body), og Geist (UI/sans) fra Google Fonts.
   Display=swap for at undgå FOIT; preconnect i <head>. */
@import url('https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght,SOFT@0,9..144,300..700,0..100;1,9..144,300..700,0..100&family=Source+Serif+4:ital,opsz,wght@0,8..60,200..900;1,8..60,200..900&family=Geist:wght@300..700&display=swap');

:root {
  --font-display: 'Fraunces', 'Iowan Old Style', Georgia, serif;
  --font-serif:   'Source Serif 4', 'Iowan Old Style', Georgia, serif;
  --font-sans:    'Geist', -apple-system, system-ui, sans-serif;
}
```

- [ ] **Step 2: Commit**

```bash
git add site/src/styles/fonts.css
git commit -m "feat(site): add font tokens (Fraunces + Source Serif 4 + Geist)"
```

---

## Task 5: Global base CSS

**Files:**
- Create: `site/src/styles/base.css`

- [ ] **Step 1: Skriv `site/src/styles/base.css`**

```css
@import './tokens.css';
@import './fonts.css';

*, *::before, *::after { box-sizing: border-box; }

html, body { margin: 0; padding: 0; }

html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  background: var(--cream);
  color: var(--ink);
  font-family: var(--font-sans);
  font-feature-settings: "ss01", "cv11";
  line-height: 1.5;
  min-height: 100vh;
}

::selection { background: var(--clay); color: var(--cream); }

a { color: inherit; text-decoration: none; }

img { max-width: 100%; height: auto; display: block; }

.container {
  max-width: var(--container);
  margin: 0 auto;
  padding-left: var(--pad-x);
  padding-right: var(--pad-x);
}

@media (max-width: 720px) {
  :root { --pad-x: 24px; }
}
```

- [ ] **Step 2: Commit**

```bash
git add site/src/styles/base.css
git commit -m "feat(site): add base global styles"
```

---

## Task 6: Base layout

**Files:**
- Create: `site/src/layouts/Base.astro`

- [ ] **Step 1: Skriv `site/src/layouts/Base.astro`**

```astro
---
import '../styles/base.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { title, description = 'Philip Birkenborg — in-house jurist der bygger AI-systemer i sundhedssektoren.', ogImage } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site).toString();
---
<!doctype html>
<html lang="da">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonical} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content="website" />
    {ogImage && <meta property="og:image" content={ogImage} />}
    <meta name="twitter:card" content="summary_large_image" />
    <slot name="head" />
  </head>
  <body>
    <slot />
  </body>
</html>
```

- [ ] **Step 2: Sæt `site` i astro.config.mjs**

Erstat `site/astro.config.mjs` med:

```js
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://birkenborg.dev',
  trailingSlash: 'never',
  build: { format: 'directory' },
});
```

- [ ] **Step 3: Verificér build**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm run build
```

Forventet: build success.

- [ ] **Step 4: Commit**

```bash
git add site/src/layouts/Base.astro site/astro.config.mjs
git commit -m "feat(site): add Base layout with meta + canonical"
```

---

## Task 7: Header component

**Files:**
- Create: `site/src/components/Header.astro`

- [ ] **Step 1: Skriv `site/src/components/Header.astro`**

```astro
---
const links = [
  { href: '/skrifter', label: 'Skrifter' },
  { href: '/projekter', label: 'Projekter' },
  { href: '/cv', label: 'CV' },
  { href: '/kontakt', label: 'Kontakt' },
];
---
<header class="site-header">
  <div class="container">
    <a class="wordmark" href="/">birkenborg<span class="slash">/</span>dev</a>
    <nav class="primary">
      {links.map(l => <a href={l.href}>{l.label}</a>)}
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky; top: 0; z-index: 50;
    background: rgba(250, 249, 245, .85);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--gray-100);
  }
  .site-header .container {
    display: flex; justify-content: space-between; align-items: center;
    padding-top: 18px; padding-bottom: 18px;
  }
  .wordmark {
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 400;
    letter-spacing: -.01em;
    font-variation-settings: 'opsz' 100, 'SOFT' 100;
  }
  .wordmark .slash { color: var(--clay); font-style: italic; }
  nav.primary { display: flex; gap: 32px; font-size: 14px; color: var(--gray-700); }
  nav.primary a { padding: 4px 0; transition: color .2s; }
  nav.primary a:hover { color: var(--ink); }
  @media (max-width: 720px) {
    nav.primary { gap: 18px; font-size: 13px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/components/Header.astro
git commit -m "feat(site): add Header component with sticky blur"
```

---

## Task 8: Footer component

**Files:**
- Create: `site/src/components/Footer.astro`

- [ ] **Step 1: Skriv `site/src/components/Footer.astro`**

```astro
---
const year = new Date().getFullYear();
---
<footer class="site-footer">
  <div class="container">
    <div class="signature">
      Philip Birkenborg<br />
      <em>Cand.merc.jur.</em>
    </div>
    <div>
      <h5>Sider</h5>
      <ul>
        <li><a href="/skrifter">Skrifter</a></li>
        <li><a href="/projekter">Projekter</a></li>
        <li><a href="/cv">CV</a></li>
      </ul>
    </div>
    <div>
      <h5>Andre steder</h5>
      <ul>
        <li><a href="https://linkedin.com/in/philip-birkenborg-509794172/" target="_blank" rel="noopener">LinkedIn</a></li>
        <li><a href="https://retsklar.dk" target="_blank" rel="noopener">retsklar.dk</a></li>
        <li><a href="mailto:birkenborg.p@hotmail.com">E-mail</a></li>
      </ul>
    </div>
    <div class="colophon">
      <span>© {year} · Bygget med Astro, hostet på Cloudflare</span>
      <span>København</span>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: 96px;
    border-top: 1px solid var(--gray-100);
    background: var(--cream-warm);
    padding: 44px 0 32px;
  }
  .site-footer .container {
    display: grid; grid-template-columns: 2fr 1fr 1fr; gap: 32px;
  }
  .signature {
    font-family: var(--font-display);
    font-size: 28px;
    font-weight: 350;
    line-height: 1.2;
    font-variation-settings: 'opsz' 144;
  }
  .signature em { color: var(--clay); font-style: italic; }
  h5 {
    font-size: 11px; letter-spacing: .15em; text-transform: uppercase;
    color: var(--gray-700); margin: 0 0 14px; font-weight: 600;
  }
  ul { list-style: none; padding: 0; margin: 0; }
  li { margin-bottom: 8px; font-size: 14px; }
  li a:hover { color: var(--clay-deep); }
  .colophon {
    grid-column: 1/-1;
    margin-top: 36px; padding-top: 20px;
    border-top: 1px solid var(--gray-100);
    display: flex; justify-content: space-between;
    font-size: 12px; color: var(--gray-400);
  }
  @media (max-width: 720px) {
    .site-footer .container { grid-template-columns: 1fr; gap: 24px; }
    .colophon { flex-direction: column; gap: 6px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/components/Footer.astro
git commit -m "feat(site): add Footer component"
```

---

## Task 9: Content collections config

**Files:**
- Create: `site/src/content.config.ts`

- [ ] **Step 1: Skriv `site/src/content.config.ts`**

```ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const posts = defineCollection({
  loader: glob({ pattern: '**/*.md', base: '../content/posts' }),
  schema: z.object({
    title: z.string(),
    slug: z.string().optional(),
    publish_at: z.coerce.date(),
    status: z.enum(['draft', 'scheduled', 'published', 'aborted']).default('draft'),
    tags: z.array(z.string()).default([]),
    privacy_flag: z.boolean().default(false),
    linkedin_url: z.string().nullable().default(null),
    excerpt: z.string().optional(),
  }),
});

const projekter = defineCollection({
  loader: glob({ pattern: '**/*.md', base: '../content/projekter' }),
  schema: z.object({
    title: z.string(),
    status: z.enum(['shipped', 'alpha', 'internal', 'archived']),
    pill_label: z.string(),
    summary: z.string(),
    featured: z.boolean().default(false),
    order: z.number().default(100),
    external_url: z.string().url().optional(),
  }),
});

export const collections = { posts, projekter };
```

- [ ] **Step 2: Commit**

```bash
git add site/src/content.config.ts
git commit -m "feat(site): add content collections config (posts, projekter)"
```

---

## Task 10: Seed posts (3 markdown-filer)

**Files:**
- Create: `content/posts/ma-agent-paragraf-30.md`, `content/posts/gdpr-klinikkaeder.md`, `content/posts/10-agents-i-produktion.md`

- [ ] **Step 1: Skriv `content/posts/ma-agent-paragraf-30.md`**

```markdown
---
title: "Hvorfor min M&A-agent fejlede på paragraf 30"
slug: "ma-agent-paragraf-30"
publish_at: 2026-05-12T09:00:00+02:00
status: published
tags: [ai, m-and-a, fejl-rapport]
privacy_flag: false
excerpt: "Jeg fodrede den med en hel virksomhedsoverdragelsesaftale og bad den finde fejl. Den fandt 14 — og overså den ene der havde betydet noget."
---

Min M&A-agent kunne læse hele aftalen, men den kunne ikke tælle.

Det er den korte version. Her er den lange.

[Resten af posten — placeholder-tekst der erstattes af Philip senere. Bevares som
seed for at sitet har realistisk content fra dag ét.]

## Hvad jeg lærte

- Sproglig præcision er ikke det samme som juridisk præcision.
- LLM'er er fantastiske til mønster-genkendelse, men de mangler stadig forretningsmæssig kontekst.
- Hver fejl er et nyt eval-eksempel.
```

- [ ] **Step 2: Skriv `content/posts/gdpr-klinikkaeder.md`**

```markdown
---
title: "GDPR i klinikkæder — tre ting jeg lærte på den hårde måde"
slug: "gdpr-klinikkaeder"
publish_at: 2026-05-04T09:00:00+02:00
status: published
tags: [gdpr, sundhedsret]
privacy_flag: false
excerpt: "Persondata-reglerne ser meget anderledes ud når man håndterer patientdata på tværs af 50 klinikker."
---

[Placeholder. Erstattes af Philip i implementation eller næste post-generation.]

## De tre læringer

1. **Dataansvar deles ikke automatisk.** Selv mellem søsterklinikker kræver overførsler en formel rolle-afklaring.
2. **Samtykke er ikke en magisk løsning.** Saglig hjemmel slår samtykke i 9 ud af 10 sundhedsdata-scenarier.
3. **Sletning er sværere end gemning.** Jo flere systemer, jo flere skygge-kopier.
```

- [ ] **Step 3: Skriv `content/posts/10-agents-i-produktion.md`**

```markdown
---
title: "10 agents i produktion — sådan er de organiseret"
slug: "10-agents-i-produktion"
publish_at: 2026-04-28T09:00:00+02:00
status: published
tags: [agents, arkitektur]
privacy_flag: false
excerpt: "Et nørdet kig ind i mit Claude Code-setup: hvilke agenter, hvad de kan, og hvad jeg har lært om at orkestrere dem."
---

[Placeholder. Erstattes ved første generation eller manuelt.]

## Listen

- contract_review
- gdpr_audit
- m-and-a_diligence
- ansaettelsesret
- sundhedsret
- selskabsret
- udbud
- compliance_scan
- doc_generator
- citation_checker
```

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev"
git add content/posts/
git commit -m "content: add 3 seed posts"
```

---

## Task 11: Seed projekter

**Files:**
- Create: `content/projekter/retsklar.md`, `content/projekter/chainhub.md`, `content/projekter/document-package.md`

- [ ] **Step 1: Skriv `content/projekter/retsklar.md`**

```markdown
---
title: "retsklar.dk"
status: shipped
pill_label: "Shipped · 2025"
summary: "AI-drevet compliance-tjek for danske SMV'er. 25 spørgsmål, fem juridiske områder, scorecard og handlingsplan."
featured: false
order: 20
external_url: "https://retsklar.dk"
---

# retsklar.dk

[Case-side: hvad det er, hvorfor jeg byggede det, hvad jeg lærte. Kort version
her — mere udfoldet i implementation.]

## Hvad

En selvbetjeningsplatform hvor små og mellemstore danske virksomheder kan få et
hurtigt overblik over deres compliance-status på tværs af GDPR, ansættelsesret,
selskabsforhold, kontrakter og bogføring.

## Hvorfor

Compliance-rådgivning er typisk dyr og rådgiver-drevet. Mange små virksomheder
kender ikke deres egne huller.

## Hvad jeg lærte

- Jura kan godt formidles som UX.
- AI-genererede rapporter skal kunne forklare *hvorfor* — ellers stoler folk ikke på dem.
- Pris-arkitektur driver brug mere end teknik.
```

- [ ] **Step 2: Skriv `content/projekter/chainhub.md`**

```markdown
---
title: "ChainHub"
status: alpha
pill_label: "Alpha · Q4 2026"
summary: "Multi-agent SaaS bygget til juridiske teams i regulerede sektorer. Jeg lærer undervejs præcist hvor grænserne for autonomi går — og hvor mennesker skal blive ind."
featured: true
order: 10
---

# ChainHub

[Mere indhold når alpha shipper. Lige nu er dette en placeholder med teaser.]

## Status

- Multi-agent kerne implementeret
- Authentication + workspace-model bygget
- Første pilot-kunder identificeret

## Hvad jeg lærer undervejs

- Multi-agent ≠ flere bedre svar. Orkestrering er kernen.
- Eval-data fra rigtige sager slår syntetiske test-cases hver gang.
- Brugerne vil have *forudsigelighed* mere end *kraft*.
```

- [ ] **Step 3: Skriv `content/projekter/document-package.md`**

```markdown
---
title: "Document package"
status: internal
pill_label: "Internal · v3"
summary: "Pipeline der genererer juridiske dokumenter i korrekt format automatisk. Bruger templates, custom skills og en god dosis fejltolerance."
featured: false
order: 30
---

# Document package

[Internt værktøj. Beskrivelse kommer senere.]

## Pipeline-overblik

1. Trigger fra Outlook-mail eller manuel input
2. Template-valg via classifier
3. Variable-extraction fra kilde-dokumenter
4. Render til DOCX/PDF i korrekt firma-format
5. Output til SharePoint i auto-genereret folder
```

- [ ] **Step 4: Commit**

```bash
git add content/projekter/
git commit -m "content: add 3 seed projekter (retsklar, chainhub, document-package)"
```

---

## Task 12: CV markdown

**Files:**
- Create: `content/cv.md`

- [ ] **Step 1: Skriv `content/cv.md`** (data-trukket fra Philips eksisterende PDF, opdateret med Tandlægen.dk siden 06/2024)

```markdown
---
title: CV
updated: 2026-05-05
---

# Philip Birkenborg Andersen

**Cand.merc.(jur.)** · Legal Counsel · København

## Erfaring

### Legal Counsel — Tandlægen.dk
**06/2024 — nu** · København
- Stort set alt juridisk arbejde i en dansk sundheds-virksomhed: selskabsret, kontrakter, sundhedsregulering, M&A, GDPR.
- Strategisk rådgivning til ledelsen.

### Fagansvarlig for investering — Lån og Spar Bank
**12/2023 — 06/2024** · København
- Strategisk, forretningsorienteret rådgivning til direktion, afdelinger og rådgivere.
- Udvikling af systemer der understøtter bankens investeringsunivers.
- Forretningsprocedurer baseret på ny lovgivning og Finanstilsyns-undersøgelser.

### Fuldmægtig — Erhvervsstyrelsen
**06/2022 — 11/2023** · København
- Strategisk juridisk rådgivning til ledelsesteams og afdelinger.
- Specialisering i investeringskontrol, erhvervsmæssige anliggender og offentlige indkøb.
- Bidrag til lovgivningsarbejdet.

### AML & Sanctions Specialist — Arbejdernes Landsbank
**01/2022 — 06/2022** · København

### Studentermedhjælper — Københavns Kommune
**04/2020 — 10/2021** · København
- Udbudsret, GDPR, kontrakt- og leverandørstyring.

### AML & Sanctions Specialist (fuldtid) — Nordea
**08/2017 — 07/2019** · Taastrup

## Uddannelse

- **Cand.merc.jur.** — Aarhus BSS, 2019–2021
- **HA(jur)** — Aalborg Universitet, 2014–2017

## Sprog

- **Dansk** — modersmål
- **Engelsk** — flydende

## Værktøjer

Office (superbruger), Karnov (superbruger), Outlook (superbruger), Claude Code, Python, GitHub Actions.
```

- [ ] **Step 2: Commit**

```bash
git add content/cv.md
git commit -m "content: add CV markdown (Tandlægen.dk 06/2024 → nu)"
```

---

## Task 13: Date-formatting utility (TDD)

**Files:**
- Create: `site/src/lib/format-date.ts`, `site/src/lib/format-date.test.ts`

- [ ] **Step 1: Skriv det fejlende test**

`site/src/lib/format-date.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { formatDate, formatDateShort } from './format-date';

describe('formatDate', () => {
  it('formaterer dato på dansk lang form', () => {
    const d = new Date('2026-05-12T09:00:00+02:00');
    expect(formatDate(d)).toBe('12. maj 2026');
  });

  it('håndterer streng-input', () => {
    expect(formatDate('2026-05-04T09:00:00+02:00')).toBe('4. maj 2026');
  });
});

describe('formatDateShort', () => {
  it('formaterer "12 · MAJ" til skrifter-liste', () => {
    const d = new Date('2026-05-12T09:00:00+02:00');
    expect(formatDateShort(d)).toBe('12 · MAJ');
  });

  it('viser to-cifrede dage som de er', () => {
    const d = new Date('2026-04-04T09:00:00+02:00');
    expect(formatDateShort(d)).toBe('4 · APR');
  });
});
```

- [ ] **Step 2: Kør testen — skal fejle**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm run test
```

Forventet: FAIL — "Cannot find module './format-date'".

- [ ] **Step 3: Skriv minimal implementation**

`site/src/lib/format-date.ts`:

```ts
const MONTHS_DA = ['jan','feb','mar','apr','maj','jun','jul','aug','sep','okt','nov','dec'];

function asDate(input: Date | string): Date {
  return input instanceof Date ? input : new Date(input);
}

export function formatDate(input: Date | string): string {
  const d = asDate(input);
  return `${d.getDate()}. ${MONTHS_DA[d.getMonth()]} ${d.getFullYear()}`;
}

export function formatDateShort(input: Date | string): string {
  const d = asDate(input);
  return `${d.getDate()} · ${MONTHS_DA[d.getMonth()].toUpperCase()}`;
}
```

- [ ] **Step 4: Kør testen — skal passe**

```bash
npm run test
```

Forventet: PASS, 4 tests bestået.

- [ ] **Step 5: Commit**

```bash
git add site/src/lib/format-date.ts site/src/lib/format-date.test.ts
git commit -m "feat(site): add formatDate utilities (Danish)"
```

---

## Task 14: Hero-component

**Files:**
- Create: `site/src/components/Hero.astro`

- [ ] **Step 1: Skriv `site/src/components/Hero.astro`**

```astro
---
interface Props {
  eyebrow?: string;
  ctaPrimary?: { href: string; label: string };
  ctaSecondary?: { href: string; label: string };
}

const {
  eyebrow = 'Aktiv · 10 agents i produktion',
  ctaPrimary = { href: '/skrifter', label: 'Læs seneste skrift' },
  ctaSecondary = { href: '/projekter', label: 'Se projekter' },
} = Astro.props;
---
<section class="hero">
  <div class="container">
    <span class="eyebrow"><span class="dot"></span>{eyebrow}</span>
    <h1 class="headline">In-house jurist der bygger <em>de værktøjer</em> mine kolleger taler om at bygge.</h1>
    <p class="deck">Cand.merc.jur. Legal Counsel hos Tandlægen.dk. Bruger fritiden på at automatisere den juridiske hverdag — og deler hvad der virker, og præcist hvor det fejler.</p>
    <div class="cta-row">
      <a class="btn btn-primary" href={ctaPrimary.href}>{ctaPrimary.label} <span class="arrow">→</span></a>
      <a class="btn btn-secondary" href={ctaSecondary.href}>{ctaSecondary.label}</a>
    </div>
  </div>
</section>

<style>
  .hero { padding: 96px 0 72px; position: relative; }
  .eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    font-size: 12px; font-weight: 500;
    color: var(--clay-deep);
    background: rgba(217,119,87,.1);
    padding: 6px 14px;
    border-radius: 999px;
    border: 1px solid rgba(217,119,87,.2);
    margin-bottom: 28px;
    animation: fadeUp .8s ease both;
  }
  .eyebrow .dot {
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--clay);
    animation: pulse 2s infinite;
  }
  @keyframes pulse {
    0%,100% { box-shadow: 0 0 0 0 rgba(217,119,87,.5); }
    50%     { box-shadow: 0 0 0 6px rgba(217,119,87,0); }
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(8px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .headline {
    font-family: var(--font-display);
    font-weight: 350;
    font-size: clamp(48px, 6.5vw, 88px);
    line-height: 1.02;
    letter-spacing: -.025em;
    margin: 0 0 24px;
    max-width: 880px;
    font-variation-settings: 'opsz' 144, 'SOFT' 100;
    animation: fadeUp .9s .05s ease both;
  }
  .headline em { font-style: italic; color: var(--clay); font-weight: 400; }
  .deck {
    font-size: 19px; line-height: 1.55;
    color: var(--gray-700);
    max-width: 620px;
    margin: 0 0 36px;
    animation: fadeUp 1s .15s ease both;
  }
  .cta-row {
    display: flex; gap: 12px; flex-wrap: wrap;
    animation: fadeUp 1s .25s ease both;
  }
  .btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 12px 22px;
    border-radius: 999px;
    font-size: 14px; font-weight: 500;
    transition: transform .15s ease, box-shadow .2s, background .2s;
  }
  .btn-primary { background: var(--ink); color: var(--cream); }
  .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 8px 24px rgba(20,20,19,.18); }
  .btn-secondary { background: transparent; color: var(--ink); border: 1px solid var(--gray-200); }
  .btn-secondary:hover { background: var(--gray-100); }
  .btn .arrow { transition: transform .2s; }
  .btn:hover .arrow { transform: translateX(3px); }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/components/Hero.astro
git commit -m "feat(site): add Hero component with eyebrow + CTA buttons"
```

---

## Task 15: StatusPanel-component

**Files:**
- Create: `site/src/components/StatusPanel.astro`

- [ ] **Step 1: Skriv `site/src/components/StatusPanel.astro`**

```astro
---
interface Props {
  text: string;
  meta: string;
  label?: string;
}
const { text, meta, label = 'Lige nu' } = Astro.props;
---
<div class="status-panel">
  <span class="label"><span class="ldot"></span>{label}</span>
  <p class="text"><Fragment set:html={text} /></p>
  <span class="meta">{meta}</span>
</div>

<style>
  .status-panel {
    margin-top: 80px;
    padding: 24px 28px;
    background: var(--cream-warm);
    border: 1px solid var(--gray-100);
    border-radius: var(--r-lg);
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 28px;
    box-shadow: var(--shadow-sm);
    animation: fadeUp 1s .35s ease both;
  }
  .label {
    font-size: 11px; letter-spacing: .12em; text-transform: uppercase;
    color: var(--gray-700); font-weight: 600;
  }
  .ldot {
    display: inline-block; width: 6px; height: 6px; border-radius: 50%;
    background: var(--sage); margin-right: 8px;
    box-shadow: 0 0 0 3px rgba(120,140,93,.18);
  }
  .text { font-family: var(--font-serif); font-size: 17px; line-height: 1.5; margin: 0; }
  .text :global(em) { color: var(--clay-deep); font-style: italic; }
  .meta { font-size: 13px; color: var(--gray-700); }
  @media (max-width: 720px) {
    .status-panel { grid-template-columns: 1fr; gap: 8px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/components/StatusPanel.astro
git commit -m "feat(site): add StatusPanel component"
```

---

## Task 16: ProjectCard-component

**Files:**
- Create: `site/src/components/ProjectCard.astro`

- [ ] **Step 1: Skriv `site/src/components/ProjectCard.astro`**

```astro
---
interface Props {
  href: string;
  title: string;
  pillLabel: string;
  status: 'shipped' | 'alpha' | 'internal' | 'archived';
  summary: string;
  featured?: boolean;
}
const { href, title, pillLabel, status, summary, featured = false } = Astro.props;
const pillClass = `pill ${status}`;
---
<a class={`project ${featured ? 'featured' : ''}`} href={href}>
  <span class={pillClass}>{pillLabel}</span>
  <h3>{title}</h3>
  <p>{summary}</p>
  <span class="arrow-small">Læs mere →</span>
</a>

<style>
  .project {
    background: var(--cream-warm);
    border: 1px solid var(--gray-100);
    border-radius: var(--r-lg);
    padding: 28px;
    display: flex; flex-direction: column; gap: 14px;
    transition: transform .2s, box-shadow .2s, border-color .2s;
    position: relative; overflow: hidden;
  }
  .project:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-hov);
    border-color: var(--gray-200);
  }
  .pill {
    align-self: flex-start;
    font-size: 11px; letter-spacing: .1em; text-transform: uppercase;
    padding: 4px 10px; border-radius: 999px; font-weight: 600;
  }
  .pill.shipped  { background: rgba(120,140,93,.16); color: var(--sage); }
  .pill.alpha    { background: rgba(217,119,87,.14); color: var(--clay-deep); }
  .pill.internal { background: rgba(106,155,204,.14); color: #3a6a99; }
  .pill.archived { background: var(--gray-100); color: var(--gray-700); }
  h3 {
    font-family: var(--font-display);
    font-size: 26px; font-weight: 400; margin: 0;
    line-height: 1.15; letter-spacing: -.01em;
  }
  p {
    font-size: 14px; color: var(--gray-700); line-height: 1.55;
    margin: 0; flex-grow: 1;
  }
  .arrow-small {
    align-self: flex-start;
    font-size: 13px; color: var(--clay-deep); font-weight: 500;
    display: inline-flex; align-items: center; gap: 6px;
    transition: gap .2s;
  }
  .project:hover .arrow-small { gap: 12px; }

  .project.featured { background: var(--ink); color: var(--cream); border-color: var(--ink); }
  .project.featured h3 { color: var(--cream); }
  .project.featured p { color: rgba(250,249,245,.7); }
  .project.featured .arrow-small { color: var(--clay-soft); }
  .project.featured .pill.alpha { background: rgba(217,119,87,.22); color: var(--clay-soft); }
  .project.featured::after {
    content: ""; position: absolute; right: -40px; bottom: -40px;
    width: 200px; height: 200px;
    background: radial-gradient(circle, rgba(217,119,87,.25) 0%, transparent 70%);
    pointer-events: none;
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/components/ProjectCard.astro
git commit -m "feat(site): add ProjectCard with featured variant + status pills"
```

---

## Task 17: WritingItem-component

**Files:**
- Create: `site/src/components/WritingItem.astro`

- [ ] **Step 1: Skriv `site/src/components/WritingItem.astro`**

```astro
---
import { formatDateShort } from '../lib/format-date';

interface Props {
  href: string;
  title: string;
  date: Date | string;
  readTime?: string;
}
const { href, title, date, readTime = '5 min' } = Astro.props;
---
<a class="writing" href={href}>
  <span class="date">{formatDateShort(date)}</span>
  <h4>{title}</h4>
  <span class="read">{readTime}</span>
</a>

<style>
  .writing {
    display: grid;
    grid-template-columns: 80px 1fr auto;
    align-items: baseline;
    gap: 32px;
    padding: 26px 0;
    border-bottom: 1px solid var(--gray-100);
    transition: padding .25s ease;
  }
  .writing:hover { padding-left: 12px; }
  .writing:hover h4 { color: var(--clay-deep); }
  .date {
    font-size: 13px;
    font-variant-numeric: tabular-nums;
    color: var(--gray-400);
  }
  h4 {
    font-family: var(--font-serif);
    font-weight: 500;
    font-size: 22px;
    margin: 0;
    line-height: 1.3;
    transition: color .2s;
  }
  .read { font-size: 12px; color: var(--gray-400); font-variant-numeric: tabular-nums; }
  @media (max-width: 720px) {
    .writing { grid-template-columns: 64px 1fr; gap: 16px; }
    .read { display: none; }
    h4 { font-size: 18px; }
  }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/components/WritingItem.astro
git commit -m "feat(site): add WritingItem component"
```

---

## Task 18: Forside (index.astro)

**Files:**
- Create: `site/src/pages/index.astro`

- [ ] **Step 1: Skriv `site/src/pages/index.astro`**

```astro
---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Hero from '../components/Hero.astro';
import StatusPanel from '../components/StatusPanel.astro';
import ProjectCard from '../components/ProjectCard.astro';
import WritingItem from '../components/WritingItem.astro';

const allPosts = await getCollection('posts', ({ data }) => data.status === 'published');
const recentPosts = allPosts
  .sort((a, b) => +b.data.publish_at - +a.data.publish_at)
  .slice(0, 4);

const allProjects = await getCollection('projekter', ({ data }) => data.status !== 'archived');
const sortedProjects = allProjects.sort((a, b) => a.data.order - b.data.order);
const featuredProjects = sortedProjects.slice(0, 3);
---
<Base title="Philip Birkenborg — birkenborg.dev">
  <Header />
  <main>
    <Hero />

    <section class="status-section">
      <div class="container">
        <StatusPanel
          text="Bygger <em>ChainHub</em> — multi-agent SaaS for juridiske teams. Alpha forventet Q4 2026."
          meta="Opdateret 5. maj"
        />
      </div>
    </section>

    <section class="work">
      <div class="container">
        <div class="section-head">
          <h2>Hvad jeg har bygget</h2>
          <a class="more" href="/projekter">Alle projekter →</a>
        </div>
        <div class="grid-projects">
          {featuredProjects.map(p => (
            <ProjectCard
              href={`/projekter/${p.id.replace(/\.md$/, '')}`}
              title={p.data.title}
              pillLabel={p.data.pill_label}
              status={p.data.status}
              summary={p.data.summary}
              featured={p.data.featured}
            />
          ))}
        </div>
      </div>
    </section>

    <section class="writings">
      <div class="container">
        <div class="section-head">
          <h2>Seneste skrifter</h2>
          <a class="more" href="/skrifter">Alle skrifter →</a>
        </div>
        <div class="writings-list">
          {recentPosts.map(p => (
            <WritingItem
              href={`/skrifter/${p.id.replace(/\.md$/, '')}`}
              title={p.data.title}
              date={p.data.publish_at}
            />
          ))}
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Base>

<style>
  .status-section { padding-top: 0; }
  .work { padding: 96px 0 24px; }
  .writings { padding: 72px 0 96px; }
  .section-head {
    display: flex; align-items: end; justify-content: space-between;
    margin-bottom: 36px;
    padding-bottom: 18px;
    border-bottom: 1px solid var(--gray-100);
  }
  .section-head h2 {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: 36px;
    margin: 0;
    letter-spacing: -.015em;
    font-variation-settings: 'opsz' 144;
  }
  .more {
    font-size: 14px; color: var(--gray-700);
    display: inline-flex; align-items: center; gap: 6px;
  }
  .more:hover { color: var(--ink); }
  .grid-projects {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 18px;
  }
  @media (max-width: 900px) {
    .grid-projects { grid-template-columns: 1fr; }
  }
</style>
```

- [ ] **Step 2: Verificér build**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm run build
```

Forventet: build success, `dist/index.html` indeholder hero-headline.

- [ ] **Step 3: Verificér visuelt (dev server)**

```bash
npm run dev
```

Åbn `http://localhost:4321` — forsiden skal matche mockup'en (cream-baggrund, italic Fraunces, clay-orange accenter, ChainHub featured-card mørk).

- [ ] **Step 4: Commit**

```bash
git add site/src/pages/index.astro
git commit -m "feat(site): wire forside (hero, status, projekter, skrifter)"
```

---

## Task 19: /skrifter index-side

**Files:**
- Create: `site/src/pages/skrifter/index.astro`

- [ ] **Step 1: Skriv `site/src/pages/skrifter/index.astro`**

```astro
---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import WritingItem from '../../components/WritingItem.astro';

const allPosts = await getCollection('posts', ({ data }) => data.status === 'published');
const sorted = allPosts.sort((a, b) => +b.data.publish_at - +a.data.publish_at);
---
<Base title="Skrifter · birkenborg.dev" description="Korte essays om jura, AI-systemer i drift og fejl jeg er løbet ind i.">
  <Header />
  <main class="page">
    <div class="container">
      <header class="page-head">
        <h1>Skrifter</h1>
        <p>Korte essays om jura, AI-systemer i drift og fejl jeg er løbet ind i. {sorted.length} stk.</p>
      </header>
      <div class="writings-list">
        {sorted.map(p => (
          <WritingItem
            href={`/skrifter/${p.id.replace(/\.md$/, '')}`}
            title={p.data.title}
            date={p.data.publish_at}
          />
        ))}
      </div>
    </div>
  </main>
  <Footer />
</Base>

<style>
  .page { padding: 64px 0 96px; }
  .page-head { margin-bottom: 48px; max-width: 720px; }
  .page-head h1 {
    font-family: var(--font-display);
    font-size: clamp(48px, 6vw, 72px);
    font-weight: 350;
    letter-spacing: -.02em;
    margin: 0 0 16px;
    font-variation-settings: 'opsz' 144;
  }
  .page-head p { font-size: 18px; color: var(--gray-700); margin: 0; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/pages/skrifter/index.astro
git commit -m "feat(site): add /skrifter index page"
```

---

## Task 20: /skrifter/[slug] dynamisk side

**Files:**
- Create: `site/src/pages/skrifter/[...slug].astro`

- [ ] **Step 1: Skriv `site/src/pages/skrifter/[...slug].astro`**

```astro
---
import { getCollection, render } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { formatDate } from '../../lib/format-date';

export async function getStaticPaths() {
  const posts = await getCollection('posts', ({ data }) => data.status === 'published');
  return posts.map(p => ({
    params: { slug: p.id.replace(/\.md$/, '') },
    props: { post: p },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
---
<Base title={`${post.data.title} · birkenborg.dev`} description={post.data.excerpt}>
  <Header />
  <main class="post-page">
    <article class="container">
      <header class="post-head">
        <p class="meta">{formatDate(post.data.publish_at)}</p>
        <h1>{post.data.title}</h1>
        {post.data.excerpt && <p class="excerpt">{post.data.excerpt}</p>}
      </header>
      <div class="post-body">
        <Content />
      </div>
      {post.data.linkedin_url && (
        <footer class="post-foot">
          <a href={post.data.linkedin_url} target="_blank" rel="noopener">Læs også på LinkedIn →</a>
        </footer>
      )}
    </article>
  </main>
  <Footer />
</Base>

<style>
  .post-page { padding: 64px 0 96px; }
  .post-head { max-width: 720px; margin: 0 auto 48px; }
  .meta {
    font-size: 12px; letter-spacing: .15em; text-transform: uppercase;
    color: var(--gray-700); margin: 0 0 16px;
  }
  .post-head h1 {
    font-family: var(--font-display);
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 350;
    line-height: 1.1;
    letter-spacing: -.02em;
    margin: 0 0 20px;
    font-variation-settings: 'opsz' 144;
  }
  .excerpt {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 19px; line-height: 1.55;
    color: var(--gray-700);
    margin: 0;
  }
  .post-body {
    max-width: 720px; margin: 0 auto;
    font-family: var(--font-serif);
    font-size: 18px; line-height: 1.7;
  }
  .post-body :global(h2) {
    font-family: var(--font-display);
    font-size: 28px; font-weight: 400;
    margin: 48px 0 16px;
    font-variation-settings: 'opsz' 144;
  }
  .post-body :global(p) { margin: 0 0 24px; }
  .post-body :global(a) { color: var(--clay-deep); text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 3px; }
  .post-body :global(ul), .post-body :global(ol) { margin: 0 0 24px; padding-left: 1.4em; }
  .post-body :global(li) { margin-bottom: 8px; }
  .post-body :global(code) {
    font-family: 'Geist Mono', ui-monospace, monospace;
    font-size: .92em;
    background: var(--gray-100);
    padding: 1px 6px; border-radius: 4px;
  }
  .post-foot {
    max-width: 720px; margin: 64px auto 0;
    padding-top: 24px; border-top: 1px solid var(--gray-100);
    font-size: 14px;
  }
  .post-foot a { color: var(--clay-deep); }
</style>
```

- [ ] **Step 2: Verificér build genererer alle skrift-sider**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm run build
ls dist/skrifter/
```

Forventet: tre mapper (`ma-agent-paragraf-30/`, `gdpr-klinikkaeder/`, `10-agents-i-produktion/`) hver med `index.html`.

- [ ] **Step 3: Commit**

```bash
git add site/src/pages/skrifter/
git commit -m "feat(site): add /skrifter/[slug] dynamic page"
```

---

## Task 21: /projekter index-side

**Files:**
- Create: `site/src/pages/projekter/index.astro`

- [ ] **Step 1: Skriv `site/src/pages/projekter/index.astro`**

```astro
---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ProjectCard from '../../components/ProjectCard.astro';

const all = await getCollection('projekter', ({ data }) => data.status !== 'archived');
const sorted = all.sort((a, b) => a.data.order - b.data.order);
---
<Base title="Projekter · birkenborg.dev" description="Hvad jeg har bygget — herunder retsklar.dk og ChainHub i alpha.">
  <Header />
  <main class="page">
    <div class="container">
      <header class="page-head">
        <h1>Projekter</h1>
        <p>Hvad jeg har shipped, hvad jeg er ved at bygge, og hvad jeg lærer undervejs.</p>
      </header>
      <div class="grid">
        {sorted.map(p => (
          <ProjectCard
            href={`/projekter/${p.id.replace(/\.md$/, '')}`}
            title={p.data.title}
            pillLabel={p.data.pill_label}
            status={p.data.status}
            summary={p.data.summary}
            featured={p.data.featured}
          />
        ))}
      </div>
    </div>
  </main>
  <Footer />
</Base>

<style>
  .page { padding: 64px 0 96px; }
  .page-head { margin-bottom: 48px; max-width: 720px; }
  .page-head h1 {
    font-family: var(--font-display);
    font-size: clamp(48px, 6vw, 72px);
    font-weight: 350;
    letter-spacing: -.02em;
    margin: 0 0 16px;
    font-variation-settings: 'opsz' 144;
  }
  .page-head p { font-size: 18px; color: var(--gray-700); margin: 0; }
  .grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
  }
  @media (max-width: 720px) { .grid { grid-template-columns: 1fr; } }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/pages/projekter/index.astro
git commit -m "feat(site): add /projekter index page"
```

---

## Task 22: /projekter/[slug] dynamisk side

**Files:**
- Create: `site/src/pages/projekter/[...slug].astro`

- [ ] **Step 1: Skriv `site/src/pages/projekter/[...slug].astro`**

```astro
---
import { getCollection, render } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

export async function getStaticPaths() {
  const all = await getCollection('projekter');
  return all.map(p => ({
    params: { slug: p.id.replace(/\.md$/, '') },
    props: { project: p },
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
---
<Base title={`${project.data.title} · Projekter`} description={project.data.summary}>
  <Header />
  <main class="page">
    <article class="container">
      <header class="head">
        <span class={`pill ${project.data.status}`}>{project.data.pill_label}</span>
        <h1>{project.data.title}</h1>
        <p class="summary">{project.data.summary}</p>
        {project.data.external_url && (
          <a class="external" href={project.data.external_url} target="_blank" rel="noopener">{project.data.external_url} →</a>
        )}
      </header>
      <div class="body">
        <Content />
      </div>
    </article>
  </main>
  <Footer />
</Base>

<style>
  .page { padding: 64px 0 96px; }
  .head { max-width: 720px; margin: 0 auto 48px; }
  .pill {
    display: inline-block;
    font-size: 11px; letter-spacing: .1em; text-transform: uppercase;
    padding: 4px 10px; border-radius: 999px; font-weight: 600;
    margin-bottom: 18px;
  }
  .pill.shipped  { background: rgba(120,140,93,.16); color: var(--sage); }
  .pill.alpha    { background: rgba(217,119,87,.14); color: var(--clay-deep); }
  .pill.internal { background: rgba(106,155,204,.14); color: #3a6a99; }
  h1 {
    font-family: var(--font-display);
    font-size: clamp(40px, 5vw, 64px);
    font-weight: 350;
    line-height: 1.1;
    letter-spacing: -.02em;
    margin: 0 0 16px;
    font-variation-settings: 'opsz' 144;
  }
  .summary { font-family: var(--font-serif); font-size: 19px; color: var(--gray-700); margin: 0 0 16px; line-height: 1.55; }
  .external { font-size: 14px; color: var(--clay-deep); text-decoration: underline; text-underline-offset: 3px; }
  .body {
    max-width: 720px; margin: 0 auto;
    font-family: var(--font-serif);
    font-size: 18px; line-height: 1.7;
  }
  .body :global(h1) { display: none; }
  .body :global(h2) {
    font-family: var(--font-display);
    font-size: 26px; font-weight: 400;
    margin: 40px 0 14px;
    font-variation-settings: 'opsz' 144;
  }
  .body :global(p) { margin: 0 0 22px; }
  .body :global(ul), .body :global(ol) { margin: 0 0 22px; padding-left: 1.4em; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/pages/projekter/
git commit -m "feat(site): add /projekter/[slug] dynamic page"
```

---

## Task 23: /cv side

**Files:**
- Create: `site/src/pages/cv.astro`

- [ ] **Step 1: Skriv `site/src/pages/cv.astro`**

```astro
---
import fs from 'node:fs/promises';
import path from 'node:path';
import { marked } from 'marked';
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

// CV.md ligger uden for src/content/ — læs den direkte fra repo-roden
const cvPath = path.resolve('../content/cv.md');
const raw = await fs.readFile(cvPath, 'utf-8');
const body = raw.replace(/^---[\s\S]*?---\n/, '');
const html = marked.parse(body);
---
<Base title="CV · birkenborg.dev" description="Philip Birkenborg Andersen, Cand.merc.jur. — Legal Counsel hos Tandlægen.dk.">
  <Header />
  <main class="page">
    <div class="container">
      <header class="head">
        <h1>Curriculum vitæ</h1>
        <p><a class="download" href="/cv.pdf" download>Download som PDF →</a></p>
      </header>
      <div class="cv-body" set:html={html} />
    </div>
  </main>
  <Footer />
</Base>

<style>
  .page { padding: 64px 0 96px; }
  .head { max-width: 720px; margin: 0 auto 48px; }
  h1 {
    font-family: var(--font-display);
    font-size: clamp(48px, 6vw, 72px);
    font-weight: 350;
    letter-spacing: -.02em;
    margin: 0 0 16px;
    font-variation-settings: 'opsz' 144;
  }
  .download { color: var(--clay-deep); text-decoration: underline; text-underline-offset: 3px; font-size: 16px; }
  .cv-body { max-width: 720px; margin: 0 auto; font-family: var(--font-serif); font-size: 17px; line-height: 1.65; }
  .cv-body :global(h1) { display: none; }
  .cv-body :global(h2) {
    font-family: var(--font-display);
    font-size: 26px; font-weight: 400;
    margin: 48px 0 16px; padding-bottom: 8px;
    border-bottom: 1px solid var(--gray-100);
  }
  .cv-body :global(h3) {
    font-family: var(--font-display);
    font-size: 20px; font-weight: 500;
    margin: 28px 0 8px;
  }
  .cv-body :global(p strong) { color: var(--gray-700); font-weight: 600; }
  .cv-body :global(ul) { margin: 0 0 16px; padding-left: 1.2em; }
  .cv-body :global(li) { margin-bottom: 6px; }
</style>
```

- [ ] **Step 2: Installer `marked` til CV-rendering**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm install marked
```

- [ ] **Step 3: Kopiér eksisterende CV-PDF til public/**

```bash
cp "C:/Users/birke/Downloads/CV - Tandlægen.dk.pdf" "C:/Users/birke/Projects/birkenborg-dev/site/public/cv.pdf"
```

- [ ] **Step 4: Verificér build**

```bash
npm run build
```

Forventet: build success, `dist/cv/index.html` indeholder CV-overskrifter.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev"
git add site/src/pages/cv.astro site/package.json site/package-lock.json site/public/cv.pdf
git commit -m "feat(site): add /cv page rendering content/cv.md"
```

---

## Task 24: /kontakt side

**Files:**
- Create: `site/src/pages/kontakt.astro`

- [ ] **Step 1: Skriv `site/src/pages/kontakt.astro`**

```astro
---
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---
<Base title="Kontakt · birkenborg.dev" description="Skriv til Philip Birkenborg.">
  <Header />
  <main class="page">
    <div class="container">
      <header class="head">
        <h1>Kontakt</h1>
      </header>
      <div class="body">
        <p>Skriv en mail til <a href="mailto:birkenborg.p@hotmail.com">birkenborg.p@hotmail.com</a> hvis du vil snakke om jura, AI-systemer i drift, eller noget tredje du tænker jeg bør have en mening om.</p>
        <p>Du kan også finde mig på <a href="https://linkedin.com/in/philip-birkenborg-509794172/" target="_blank" rel="noopener">LinkedIn</a> — jeg er der lidt for ofte.</p>
      </div>
    </div>
  </main>
  <Footer />
</Base>

<style>
  .page { padding: 64px 0 96px; }
  .head { max-width: 720px; margin: 0 auto 32px; }
  h1 {
    font-family: var(--font-display);
    font-size: clamp(48px, 6vw, 72px);
    font-weight: 350;
    letter-spacing: -.02em;
    margin: 0;
    font-variation-settings: 'opsz' 144;
  }
  .body {
    max-width: 720px; margin: 0 auto;
    font-family: var(--font-serif);
    font-size: 19px; line-height: 1.65;
  }
  .body p { margin: 0 0 20px; }
  .body a { color: var(--clay-deep); text-decoration: underline; text-underline-offset: 3px; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add site/src/pages/kontakt.astro
git commit -m "feat(site): add /kontakt page"
```

---

## Task 25: Sitemap + RSS

**Files:**
- Modify: `site/astro.config.mjs`, `site/package.json`
- Create: `site/src/pages/rss.xml.ts`

- [ ] **Step 1: Installer Astro-integrationer**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm install @astrojs/sitemap @astrojs/rss
```

- [ ] **Step 2: Opdater `site/astro.config.mjs`**

Erstat hele filen med:

```js
import { defineConfig } from 'astro/config';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://birkenborg.dev',
  trailingSlash: 'never',
  build: { format: 'directory' },
  integrations: [sitemap()],
});
```

- [ ] **Step 3: Skriv `site/src/pages/rss.xml.ts`**

```ts
import rss from '@astrojs/rss';
import { getCollection } from 'astro:content';
import type { APIRoute } from 'astro';

export const GET: APIRoute = async (context) => {
  const posts = await getCollection('posts', ({ data }) => data.status === 'published');
  const sorted = posts.sort((a, b) => +b.data.publish_at - +a.data.publish_at);

  return rss({
    title: 'birkenborg.dev — Skrifter',
    description: 'Korte essays om jura, AI-systemer i drift og fejl jeg er løbet ind i.',
    site: context.site!.toString(),
    items: sorted.map(p => ({
      title: p.data.title,
      pubDate: p.data.publish_at,
      description: p.data.excerpt ?? '',
      link: `/skrifter/${p.id.replace(/\.md$/, '')}`,
    })),
    customData: '<language>da-dk</language>',
  });
};
```

- [ ] **Step 4: Verificér build**

```bash
npm run build
ls dist/sitemap-0.xml dist/rss.xml
```

Forventet: begge filer eksisterer.

- [ ] **Step 5: Commit**

```bash
git add site/astro.config.mjs site/package.json site/package-lock.json site/src/pages/rss.xml.ts
git commit -m "feat(site): add sitemap + RSS feed"
```

---

## Task 26: JSON-LD Person schema

**Files:**
- Modify: `site/src/layouts/Base.astro`

- [ ] **Step 1: Tilføj JSON-LD-blok til `Base.astro`**

I `site/src/layouts/Base.astro`, indsæt før `</head>`:

```astro
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Person',
      'name': 'Philip Birkenborg Andersen',
      'jobTitle': 'Legal Counsel',
      'worksFor': { '@type': 'Organization', 'name': 'Tandlægen.dk' },
      'url': 'https://birkenborg.dev',
      'sameAs': [
        'https://linkedin.com/in/philip-birkenborg-509794172/',
        'https://retsklar.dk',
      ],
      'alumniOf': [
        { '@type': 'CollegeOrUniversity', 'name': 'Aarhus BSS' },
        { '@type': 'CollegeOrUniversity', 'name': 'Aalborg Universitet' },
      ],
    })} />
```

- [ ] **Step 2: Verificér build**

```bash
npm run build
grep -c "schema.org" dist/index.html
```

Forventet: tællingen er ≥ 1.

- [ ] **Step 3: Commit**

```bash
git add site/src/layouts/Base.astro
git commit -m "feat(site): add JSON-LD Person schema to Base layout"
```

---

## Task 27: Smoke-test (Playwright)

**Files:**
- Create: `site/src/tests/pages.spec.ts`

- [ ] **Step 1: Skriv `site/src/tests/pages.spec.ts`**

```ts
import { test, expect } from '@playwright/test';

test('forside renders med hero-headline', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('h1.headline')).toContainText('In-house jurist');
  await expect(page.locator('.eyebrow')).toBeVisible();
});

test('forsiden viser tre projekter', async ({ page }) => {
  await page.goto('/');
  const cards = page.locator('.project');
  await expect(cards).toHaveCount(3);
});

test('navigation til /skrifter virker', async ({ page }) => {
  await page.goto('/');
  await page.click('nav.primary a:has-text("Skrifter")');
  await expect(page).toHaveURL(/\/skrifter$/);
  await expect(page.locator('h1')).toContainText('Skrifter');
});

test('seneste post-side renders fuld titel', async ({ page }) => {
  await page.goto('/skrifter/ma-agent-paragraf-30');
  await expect(page.locator('h1')).toContainText('paragraf 30');
});

test('CV-side har download-link', async ({ page }) => {
  await page.goto('/cv');
  await expect(page.locator('a.download')).toHaveAttribute('href', '/cv.pdf');
});

test('alle sider returnerer 200', async ({ page }) => {
  for (const url of ['/', '/skrifter', '/projekter', '/cv', '/kontakt']) {
    const resp = await page.goto(url);
    expect(resp?.status()).toBe(200);
  }
});
```

- [ ] **Step 2: Kør e2e-test**

```bash
cd "C:/Users/birke/Projects/birkenborg-dev/site"
npm run e2e
```

Forventet: alle 6 tests passerer.

- [ ] **Step 3: Commit**

```bash
git add site/src/tests/pages.spec.ts
git commit -m "test(site): add e2e smoke tests for all pages"
```

---

## Task 28: Cloudflare Pages-deployment

**Files:**
- Create: `site/wrangler.toml`, `.github/workflows/deploy.yml`

**Forudsætninger:** Cloudflare-konto skal være oprettet. Token til GitHub Actions: opret API-token med `Cloudflare Pages:Edit` permission.

- [ ] **Step 1: Push lokalt repo til GitHub**

Først opret repo på GitHub manuelt eller via gh:

```bash
cd "C:/Users/birke/Projects/birkenborg-dev"
gh repo create birkenborg-dev --public --source=. --remote=origin --push
```

- [ ] **Step 2: Forbind Cloudflare Pages til repo**

I Cloudflare dashboard:
1. **Pages** → **Create application** → **Connect to Git** → vælg `birkenborg-dev`
2. Build settings:
   - Framework preset: **Astro**
   - Build command: `cd site && npm install && npm run build`
   - Build output: `site/dist`
   - Root directory: `/`
3. **Save and Deploy**

Forventet: Cloudflare bygger og deployer til en `*.pages.dev`-URL inden for 2-3 min.

- [ ] **Step 3: Test live-site**

Åbn `https://birkenborg-dev.pages.dev` (faktisk URL fra Cloudflare). Verificér:
- Forsiden renders korrekt
- Skrifter, projekter, CV, kontakt alle responder 200
- RSS på `/rss.xml` virker
- Sitemap på `/sitemap-0.xml` virker

- [ ] **Step 4: Tilføj `site` URL til astro.config**

Erstat `site: 'https://birkenborg.dev'` midlertidigt med Cloudflare-URL'en, indtil custom domain er sat op:

```js
site: 'https://birkenborg-dev.pages.dev',
```

(Skiftes tilbage når `birkenborg.dev`-domænet er pointet på Cloudflare.)

- [ ] **Step 5: Commit**

```bash
git add site/astro.config.mjs
git commit -m "chore: point site URL to pages.dev pre-domain"
git push origin main
```

---

## Task 29: Lighthouse-audit GitHub Action

**Files:**
- Create: `.github/workflows/lighthouse.yml`

- [ ] **Step 1: Skriv `.github/workflows/lighthouse.yml`**

```yaml
name: Lighthouse audit

on:
  schedule:
    - cron: '0 6 * * 1'  # mandag 06:00 UTC
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            https://birkenborg-dev.pages.dev
            https://birkenborg-dev.pages.dev/skrifter
            https://birkenborg-dev.pages.dev/projekter
            https://birkenborg-dev.pages.dev/cv
          uploadArtifacts: true
          temporaryPublicStorage: true
          configPath: ./lighthouserc.json
```

- [ ] **Step 2: Skriv `lighthouserc.json` i repo-roden**

```json
{
  "ci": {
    "collect": { "numberOfRuns": 1 },
    "assert": {
      "assertions": {
        "categories:performance":   ["error", { "minScore": 0.95 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices":["error", { "minScore": 0.95 }],
        "categories:seo":           ["error", { "minScore": 0.95 }]
      }
    }
  }
}
```

- [ ] **Step 3: Trigger workflow manuelt**

```bash
gh workflow run lighthouse.yml
```

Forventet: workflow kører, alle scores ≥ 95.

- [ ] **Step 4: Commit + push**

```bash
git add .github/workflows/lighthouse.yml lighthouserc.json
git commit -m "ci: add weekly Lighthouse audit"
git push origin main
```

---

## Task 30: README med setup-noter

**Files:**
- Create: `README.md`

- [ ] **Step 1: Skriv `README.md`**

```markdown
# birkenborg.dev

Personligt hub-website. Anthropic-æstetik. Statisk Astro-site med markdown-content.

## Status

- **Site (M1):** ✅ Live på `*.pages.dev`
- **Pipeline (M2):** Planlagt
- **Privacy gate (M3):** Planlagt

## Lokal udvikling

```bash
cd site
npm install
npm run dev
# → http://localhost:4321
```

## Bygge til produktion

```bash
cd site
npm run build
```

Output: `site/dist/`

## Tests

```bash
cd site
npm run test          # vitest unit tests
npm run e2e           # playwright e2e
```

## Indholds-struktur

- `content/posts/` — skrifter (markdown med frontmatter)
- `content/projekter/` — projekt-cases
- `content/cv.md` — CV (sandheden, renderes på /cv)

## Designsystem

Se `docs/superpowers/specs/2026-05-05-birkenborg-dev-cv-linkedin-agent-design.md` afsnit 4 for komplette design-tokens, fonte og komposition.

## Hosting

Cloudflare Pages, auto-deploy fra `main`-branch. Custom domain `birkenborg.dev` pointes på Cloudflare når registreret.
```

- [ ] **Step 2: Commit + push**

```bash
git add README.md
git commit -m "docs: add README with setup + status"
git push origin main
```

---

## Verifikation efter M1 complete

Når alle 30 tasks er færdige:

- [ ] Forsiden på `https://birkenborg-dev.pages.dev` matcher godkendt mockup
- [ ] Alle 5 hovedsider (forside, skrifter, projekter, cv, kontakt) responder 200
- [ ] Tre seed-posts og tre seed-projekter er synlige
- [ ] CV-PDF kan downloades fra `/cv.pdf`
- [ ] RSS-feed validerer på `/rss.xml`
- [ ] Sitemap inkluderer alle sider
- [ ] Lighthouse-audit: 95+ på alle fire kategorier
- [ ] Vitest passerer (`npm run test`)
- [ ] Playwright passerer (`npm run e2e`)
- [ ] Repo er public på GitHub
- [ ] Cloudflare Pages auto-deployer ved push til `main`

---

## Out of scope for M1 (kommer i M2/M3)

- LinkedIn-publishing pipeline
- Telegram-bot
- Draft-generation via Claude
- Privacy-classifier
- Friday-nudge
- Token-refresh workflow
- ChainHub-projektsiden vil først blive opdateret med rigtigt indhold når alpha shipper

## Næste skridt efter M1

Når M1 er live og verificeret, skriv plan for **M2 — Pipeline core**: agent-repo-setup, LinkedIn API-app-registrering, generate_drafts.py, publish_post.py, telegram_bot.py uden #tandlægen-gate, første live LinkedIn-post via API.
